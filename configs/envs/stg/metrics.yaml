metrics:
  provider: prometheus
  options:
    host: "0.0.0.0"
    port: 9100
    default_labels:
      environment: "stg"
    otel:
      enabled: true
      endpoint: "otel-collector.stg.svc.cluster.local:4317"

